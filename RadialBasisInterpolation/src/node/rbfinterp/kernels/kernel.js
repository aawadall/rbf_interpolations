/**
 * Kernel Interface
 * @interface
 * @param {Object} options
 * @param {function} distanceFunction
 * Should have the following methods:
 * - similarity (a, b) -> number; // returns a similarity score between points a and b
 */

// Path: kernels/linear.js

